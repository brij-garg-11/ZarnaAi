[pytest]
testpaths = tests
# Scripts in scripts/ are standalone command-line tools, not pytest modules.
# Several (test_phase*, test_tenant_isolation_*, test_db_schema_*) hit real APIs
# at import time and don't follow pytest's test_*() function convention.
norecursedirs = scripts node_modules operator/lovable-frontend lovable-frontend
