# Secrets — never commit
.env

# Large uncompressed embeddings file (use .gz version instead)
training_data/zarna_embeddings.json

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/
ENV/

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# ngrok binary and config
ngrok
.ngrok2/

# Jupyter / notebooks
.ipynb_checkpoints/

# Logs
*.log

# Frontend sub-repo — has its own .git/ and its own deploy pipeline.
# Tracked and pushed from its own repository, NEVER from this one.
lovable-frontend/

# Local debugging one-offs — keep out of the main repo.
# (Promote to a real test if reused — don't let these ride along.)
scripts/debug_*.py
scripts/audit_winning_corpus.py
